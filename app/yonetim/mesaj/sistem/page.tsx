import { YakindaEkrani } from "../_bilesenler/yakinda-ekrani";

export default function SistemSayfasi() {
  return (
    <YakindaEkrani
      baslik="Sistem"
      aciklama="Entegrasyon sağlığı, webhook olayları ve audit log — sağlayıcı bağlantıları (Netgsm, Meta, Resend) kurulunca bu ekran açılacak."
    />
  );
}
